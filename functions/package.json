{
  "author": "theostanton",
  "name": "parastats-functions",
  "version": "0.1.0",
  "main": "./index.js",
  "scripts": {
    "test": "vitest run",
    "buildProd": "tsc",
    "devApi": "tsc -p dev.tsconfig.json && nodemon --inspect src/api/dev.ts",
    "devTasks": "tsc -p dev.tsconfig.json && nodemon --inspect src/tasks/dev.ts",
    "devWebhooks": "tsc -p dev.tsconfig.json && nodemon --inspect src/webhooks/dev.ts"
  },
  "dependencies": {
    "express": "^5.0.1",
    "axios": "^1.8.4",
    "dotenv": "^16.4.7",
    "ts-postgres": "^2.0.4",
    "@google-cloud/tasks": "^6.0.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "@types/express": "^5.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1",
    "testcontainers": "^10.24.2",
    "@testcontainers/postgresql": "^10.24.2"
  }
}
